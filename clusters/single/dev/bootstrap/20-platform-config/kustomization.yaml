apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../platform/core/metallb/overlays/dev
  - ../../../../../platform/core/ingress-nginx/overlays/dev
  - ../../../../../platform/core/external-dns/overlays/dev
  - ../../../../../platform/core/step-ca/overlays/dev
  - ../../../../../platform/core/cert-manager/overlays/dev

patches:
  - target:
      kind: ConfigMap
      name: coredns
      namespace: kube-system
    patch: |-
      - op: replace
        path: /data/Corefile
        value: |
          .:53 {
              errors
              health
              ready
              kubernetes cluster.local in-addr.arpa ip6.arpa {
                pods insecure
                fallthrough in-addr.arpa ip6.arpa
              }
              hosts /etc/coredns/NodeHosts {
                ttl 60
                reload 15s
                fallthrough
              }
              prometheus :9153
              cache 30
              loop
              reload
              loadbalance
              import /etc/coredns/custom/*.override
              forward . /etc/resolv.conf
          }

          int.aethericforge.ca:5335 {
              errors
              forward . 192.168.1.1
          }

          import /etc/coredns/custom/*.server
