apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: step-ca-data
  namespace: step-ca
spec:
  storageClassName: gp3
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
