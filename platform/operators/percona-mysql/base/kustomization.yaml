apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml

helmCharts:
  - name: ps-operator
    repo: https://percona.github.io/percona-helm-charts/
    version: 1.1.0
    releaseName: percona-mysql-operator
    namespace: percona-mysql
    includeCRDs: true
